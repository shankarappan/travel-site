# AI outage

1. Keep conventional search/forms available — never force chat.
2. Disable concierge route or return graceful degradation message.
3. Preserve conversation audit logs; do not retry high-impact tools automatically.
4. Resume with prompt version checks and eval fixtures before re-enabling.
